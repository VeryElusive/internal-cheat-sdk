#include "visuals.h"

void CVisuals::Main( ) {
	//if ( !ctx.m_pLocal )
	//	return;

	// replace with highestentity (in m_pGameEntitySystem) when we do other entity visuals
	//for ( int i{ }; i < Interfaces::Engine->GetMaxClients( ); ++i ) {
	//	const auto ent{ Interfaces::GameResourceService->m_pGameEntitySystem->Get( i ) };

		//if ( );
	//}
}